

export class ingressomodel {
  nome: string;
  quantidade: number;
  filmes: string;

  constructor(nome: string, quantidade: number, filme: string) {
    this.nome = nome;
    this.quantidade = quantidade;
    this.filmes = filme;
  }
}
